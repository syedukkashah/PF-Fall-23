
#include <stdio.h>
int main(){
          int height, width, length,volume; //declaration 
          height = 20; // initializations
          width = 10;
          length = 10;
          volume = height*width*length;
          printf("Width of the container is %d cm", width);
          printf("\nLength of the container is %d cm", length);
          printf("\nHeight of the container is %d cm", height);
          printf("\nVolume of the container is %d cubic cm", volume);
}//end main
